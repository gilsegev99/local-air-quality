version: 2

macros:
  - name: get_aqi_description
    description: >
      This macro receives the air quality index value and returns the corresponding category name.
    arguments:
      - name: aqi
        type: int
        description: >
          AQI value.
          Must be one of the accepted values, otherwise the macro will return null
